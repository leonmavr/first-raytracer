#ifndef RENDERER_H
#define RENDERER_H

#include "entities.h" 

void render_solid(solid_t* solid) {
    if (solid->type == SOLID_SPHERE) {

    }
    // TODO: implement remaining solids
}

#endif // RENDERER_H
